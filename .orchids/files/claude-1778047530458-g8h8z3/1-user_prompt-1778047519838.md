You are a senior React engineer. This is the CashBook — Smart Finance app 
(React + Vite + Zustand + Recharts + Framer Motion). You have the full codebase 
in front of you. Work through every task below in order. Show the complete 
updated file after every change. Fix any error before moving to the next task. 
Do not summarize — write the actual code.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 1 — CRITICAL: FIX THE AI BACKEND (broken in production)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The @anthropic-ai/sdk is a Node.js server SDK. Calling it directly from the 
browser exposes the API key in the bundle and fails due to CORS.

Replace the current AI integration with a browser-safe approach using the 
Anthropic Messages API directly via fetch():

  const response = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: {
      'x-api-key': apiKey,               // from user-entered key in settings
      'anthropic-version': '2023-06-01',
      'anthropic-dangerous-direct-browser-access': 'true',
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: 'claude-opus-4-5',
      max_tokens: 1024,
      messages: [{ role: 'user', content: prompt }]
    })
  });

- Remove @anthropic-ai/sdk from package.json and all imports
- Create src/services/aiService.js that exports:
    callClaude(prompt, apiKey)  → uses fetch as above
    callGemini(prompt, apiKey)  → see Task 2
    callAI(prompt, settings)    → routes to the right provider based on 
                                   settings.aiProvider ('claude' | 'gemini')
- The API key is NEVER hardcoded. It comes from the app's settings/store.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 2 — ADD GEMINI API (free tier, browser-safe)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add Google Gemini as a second AI provider. Gemini has a free tier and supports 
direct browser calls via REST.

Install: npm install @google/generative-ai

In src/services/aiService.js, implement callGemini():

  import { GoogleGenerativeAI } from '@google/generative-ai';

  export async function callGemini(prompt, apiKey) {
    const genAI = new GoogleGenerativeAI(apiKey);
    const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' });
    // gemini-1.5-flash is free tier, fast, and generous on quota
    const result = await model.generateContent(prompt);
    return result.response.text();
  }

Gemini 1.5 Flash: free at https://aistudio.google.com/app/apikey
No credit card required. Add this info as a tooltip in the UI.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 3 — AI SETTINGS PANEL: COLLAPSIBLE + PROVIDER SWITCH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find the existing AI insights section/panel. It currently has no minimize 
option. Make these changes:

3a. ADD COLLAPSIBLE BEHAVIOR
- Add a minimize/chevron button to the AI panel header
- When collapsed: show only the header bar (provider name + minimize icon)
- When expanded: show the full panel
- Animate the collapse with Framer Motion (height animation)
- Persist collapsed state in Zustand or localStorage so it stays collapsed 
  on refresh

3b. ADD PROVIDER SELECTOR inside the panel (when expanded):
Build a toggle/tab switcher:

  [ Claude (Anthropic) ]  [ Gemini (Free) ]

  Claude tab:
    - Text input: "Anthropic API Key" (type=password, show/hide toggle)
    - Helper text: "Get key at console.anthropic.com"
    - Model shown: "claude-opus-4-5"

  Gemini tab:
    - Text input: "Gemini API Key" (type=password, show/hide toggle)  
    - Helper text: "Free key at aistudio.google.com/app/apikey — no card needed"
    - Model shown: "gemini-1.5-flash (free tier)"

  Both tabs:
    - "Test Connection" button → sends a test prompt "Say OK" and shows 
      ✅ Connected or ❌ Failed with the error message
    - Save button that writes to Zustand store AND localStorage

3c. ADD QUICK-ASK INPUT in the panel:
  - A text field: "Ask about your finances..."
  - Pre-built prompt buttons: 
      [💡 Spending insights]  [📊 Budget tips]  [💰 Save more]
  - On send: inject the user's real transaction data as context into the 
    prompt, then call callAI() with the selected provider
  - Show response in a scrollable card below, with a loading skeleton 
    while streaming
  - Show which model responded (e.g. "via Gemini 1.5 Flash")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 4 — PERSIST DATA TO LOCALSTORAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Right now all transactions and data reset on page refresh because Zustand 
state is in-memory only.

- Add localStorage persistence to the Zustand store using the zustand/middleware 
  persist middleware:

  import { persist } from 'zustand/middleware'

  export const useFinanceStore = create(
    persist(
      (set, get) => ({
        // ... existing state and actions
      }),
      {
        name: 'cashbook-storage',  // localStorage key
        partialize: (state) => ({  // only persist these:
          transactions: state.transactions,
          budgets: state.budgets,
          goals: state.goals,
          settings: state.settings,
        })
      }
    )
  )

- After adding persist, verify in browser DevTools → Application → 
  LocalStorage that data survives a hard refresh
- Do NOT persist aiResponses or loading states

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 5 — ADD MISSING CORE FEATURES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit the current app. If ANY of these features are missing or non-functional, 
implement them fully:

5a. BUDGET TRACKING
- If there is no budget feature: add a Budgets section
  - User can set a monthly budget per category (Food, Transport, etc.)
  - Show progress bar: spent / budget limit with color coding
    green < 70%, yellow 70-90%, red > 90%
  - Calculate automatically from transactions that month

5b. SAVINGS GOALS
- If there is no goals feature: add a Goals section
  - User can create a goal: name, target amount, target date
  - User can log contributions toward a goal
  - Show progress ring and days remaining

5c. TRANSACTION FILTERS & SEARCH
- If not present: add a search bar + filter by:
    Date range (this week / this month / this year / custom)
    Category dropdown
    Type (income / expense)
  - Results update in real time as user types/selects

5d. CATEGORY MANAGEMENT
- If categories are hardcoded: make them editable
  - User can add custom categories with an emoji/icon picker
  - User can rename or delete categories (with a warning if transactions exist)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 6 — EXPORT FUNCTIONALITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add an export button in the transactions view:

6a. CSV EXPORT
- Export all visible (filtered) transactions as a .csv file
- Columns: Date, Description, Category, Type, Amount
- Use the browser's Blob API, no library needed:
    const blob = new Blob([csvContent], { type: 'text/csv' });
    const url = URL.createObjectURL(blob);
    // trigger download

6b. PDF EXPORT (summary only — not every transaction)  
- Install: npm install jspdf
- Export a one-page summary PDF:
    Title: "CashBook Report — [Month Year]"
    Total income, total expenses, net savings
    Top 5 spending categories (amount + % of total)
    AI insight if one has been generated
- Export button lives in the dashboard/summary view

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 7 — CURRENCY SELECTOR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If the app only supports one currency (₹ or $):
- Add a currency selector in Settings: 
    INR ₹, USD $, EUR €, GBP £, JPY ¥, AED د.إ, SGD S$
- Store the selected currency symbol in Zustand (persisted)
- Replace all hardcoded currency symbols across the app with the selected one
- Format amounts using Intl.NumberFormat with the correct locale

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 8 — UX POLISH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

8a. EMPTY STATES
- If there are no transactions yet, show a friendly empty state with:
    An illustration (SVG inline, finance-themed)
    "Add your first transaction to get started"
    A prominent "+ Add Transaction" button

8b. LOADING STATES
- Any AI call must show a skeleton loader or animated dots while waiting
- Show a toast notification on success ("Insight generated ✓") and 
  on error ("AI request failed — check your API key")

8c. DARK/LIGHT MODE TOGGLE (if not already present)
- Add a sun/moon icon toggle in the header
- Store preference in localStorage
- Apply via a CSS class on <html> or <body>

8d. MOBILE RESPONSIVENESS CHECK
- The glass design must work on mobile (320px+)
- Bottom navigation bar on mobile instead of sidebar (if sidebar exists)
- All touch targets must be minimum 44px

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 9 — FIX THE README
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The current README.md is the default Vite template README. Replace it entirely:

# CashBook — Smart Finance

Personal finance manager with AI insights and Apple Liquid Glass design.
Live: https://ai-private-finance-glass-app.vercel.app/

## Features
[list every feature that now exists after these tasks]

## AI Setup
The app supports two AI providers — both are optional:

### Claude (Anthropic)
1. Get key at https://console.anthropic.com
2. Open the AI panel → Claude tab → paste key → Save

### Gemini (Google) — Free tier
1. Get free key at https://aistudio.google.com/app/apikey
2. No credit card required
3. Open the AI panel → Gemini tab → paste key → Save

## Local Development
  npm install
  npm run dev

## Tech Stack
React 19, Vite 8, Zustand, Recharts, Framer Motion, 
Anthropic API, Google Gemini API, Lucide icons

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TASK 10 — FINAL BUILD CHECK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run: npm run build

If it errors, fix every error until the build succeeds cleanly.
Then run: npm run preview and verify the app loads and all sections render.

FINAL CHECKLIST — do not close until all are checked:

[ ] npm run build — zero errors, zero warnings about missing imports
[ ] AI panel has a working minimize/collapse button
[ ] Both Claude and Gemini tabs appear in the AI settings
[ ] "Test Connection" button works for Gemini (free key)
[ ] Gemini model used is gemini-1.5-flash (free tier)
[ ] Transactions survive a hard browser refresh (localStorage persist)
[ ] CSV export downloads a valid file
[ ] PDF export downloads a one-page summary
[ ] Currency selector in settings changes symbols app-wide
[ ] Budget progress bars show correctly
[ ] README describes both AI providers with setup instructions
[ ] No API keys hardcoded anywhere in source files
[ ] No @anthropic-ai/sdk import anywhere (replaced with fetch)